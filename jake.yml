---
source_directory:       .
build_directory:        .
layout:                 together

builds:
  min:
    shrink_vars:        true
    private:            true

packages:
  location-picker:
    files:
      - location-picker
    meta:
      requires:
        - JS.MethodChain
        - JS.Class
        - GMap2
        - GClientGeocoder
        - GEvent
        - GLatLng
        - GMarker
      uses:
        - Ojay
        - Ojay.HTML
      provides:
        - LocationPicker

